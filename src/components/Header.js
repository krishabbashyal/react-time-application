import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <div className="header-div">
      <h1>
        Can we predict when Suman will <i> actually</i> get on?
      </h1>
      <h3>Using the power of JavaScript, React, and a little bit of Luck.</h3>
      <h3>
        We will try to guess exactly when Suman will join the voice channel.
      </h3>
    </div>
  );
};

export default Header;
